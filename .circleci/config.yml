version: 2.1
jobs:
  build:
    docker:
      # Primary container image where all steps run
      - image: cimg/node:20.5.0
    steps: 
      - checkout
      - run: node --version
      - run: yarn
      - run: yarn test